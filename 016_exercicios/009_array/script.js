// Define a frase
let frase = "O rato roeu a roupa do rei de Roma";

// Converte a frase em um array de palavras
let palavras = frase.split(" ");

// Imprime cada palavra no console
for (let i = 0; i < palavras.length; i++){
console.log(palavras[i]);
}