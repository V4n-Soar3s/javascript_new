//Propriedades

//Declaro uma variável e atribuo um valor ex:
let nome = 'João';

//podemos ver quantas letras tem em joao com o método length, ex: 
console.log(nome.length);

//Quando criamos um array de números tbm podemos ver quantos numeros existem dentro dele ex: 

let numeros = [1, 2, 3, 4, 23, 24, 5, 6, 7, 8]; 

console.log(numeros.length);//tudo o que vemos depois de um . (algum outro nome/variavel) é uma propriedade;
console.log(numeros[1]);//indices tbm sao propriedades de array
console.log(numeros['length']);//isso tbm seria uma propriedade